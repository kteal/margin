{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Margin Extension",
      "type": "extensionHost",
      "request": "launch",
      "preLaunchTask": "npm: compile",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}"
      ],
      "outFiles": [
        "${workspaceFolder}/out/**/*.js"
      ]
    }
  ]
}
